generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = "mysql://6nijcz8gwf6zomun1iex:pscale_pw_2AcNwB2PbyeYT2tWfqaFGDzofJDewtF9Au754VeuoH2@aws.connect.psdb.cloud/geostickers?sslaccept=strict"
}

model QR {
    id          String   @id @default(cuid())
    index       Int      @unique @default(autoincrement())
    createdAt   DateTime @default(now())
    lastScanned DateTime @updatedAt
    scans       Int      @default(0)
}
